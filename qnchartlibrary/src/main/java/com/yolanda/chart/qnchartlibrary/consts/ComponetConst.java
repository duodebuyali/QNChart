package com.yolanda.chart.qnchartlibrary.consts;

import android.graphics.Color;

/**
 * @author: hekang
 * @description: 使用在组件中的常量
 * @date: 2018/11/21 19:06
 */
public interface ComponetConst {

    /**
     * 文本的默认颜色
     */
    int DEFAULT_TEXT_COLOR = Color.BLACK;
    /**
     * 线的默认颜色
     */
    int DEFAULT_LINE_COLOR = Color.BLACK;

    /**
     * 默认的额外空间
     */
    int DEFAULT_EXTRA_SPACE_DP = 1;
    /**
     * 默认的绘制数据点时，图形的第一个宽度
     */
    int DEFAULT_POINT_FIRST_WIDTH_DP = 3;
    /**
     * 默认的绘制数据点时，图形的第二个宽度
     */
    int DEFAULT_POINT_SECOND_WIDTH_DP = 1;

    /**
     * 默认的线的宽度
     */
    float DEFAULT_LINE_WIDTH_DP = 0.5f;

}
